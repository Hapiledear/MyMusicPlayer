<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="5dp"
    android:paddingLeft="20dp"
    android:paddingTop="5dp" >

    <ImageView
        android:id="@+id/notifi_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/ic_launcher" />

    <TextView
        android:id="@+id/notifi_song"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/notifi_icon"
        android:layout_marginLeft="15dp"
        android:layout_toRightOf="@id/notifi_icon"
        android:text="歌曲名"
        android:textColor="#ffffff"
        android:textSize="20sp"
        android:singleLine="true" />

    <TextView
        android:id="@+id/notifi_artist"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/notifi_song"
        android:layout_below="@id/notifi_song"
        android:text="歌手名"
        android:textColor="#cecbcb"
        android:textSize="16sp"
        android:singleLine="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="next"
        android:id="@+id/noti_btn_next"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="暂停"
        android:id="@+id/noti_btn_play"
        android:layout_alignBottom="@+id/noti_btn_next"
        android:layout_toStartOf="@+id/noti_btn_next" />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="开始"
        android:id="@+id/noti_btn_pause"
        android:visibility="gone"
        android:layout_alignBottom="@+id/noti_btn_next"
        android:layout_toStartOf="@+id/noti_btn_next" />

</RelativeLayout>